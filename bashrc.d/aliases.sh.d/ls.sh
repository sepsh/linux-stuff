# Aliases for "ls"
# this files will be sources by aliases.sh

ls="ls -h --color=auto"

my_aliases[ls]="$ls"
my_aliases[l]="$ls"
my_aliases[ll]="$ls -l"
my_aliases[la]="$ls -a"
my_aliases[lla]="$ls -la"
my_aliases[lal]="$ls -al"
unset ls
